---
- name: Apply IPv4 forwarding
  ansible.builtin.command:
    argv: [sysctl, --system]
  changed_when: true

- name: Restart automation egress NAT
  ansible.builtin.systemd_service:
    name: openvpn-automation-egress.service
    state: restarted
    daemon_reload: true
